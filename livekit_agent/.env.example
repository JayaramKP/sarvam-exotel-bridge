# Copy this file to .env.local and fill in the values.
# For LOCAL TESTING only. For production, set these as LiveKit Cloud
# deployment secrets instead (see README runbook) so the agent runs
# 24/7 on LiveKit's servers and does NOT depend on your machine.

# --- LiveKit connection (project p_5vfmp4mnabg) ---
LIVEKIT_URL=wss://voice-ai-u0razux7.livekit.cloud
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# --- Sarvam (STT + LLM + TTS) ---
SARVAM_API_KEY=
